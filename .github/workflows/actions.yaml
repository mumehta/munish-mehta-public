name: build-in-action

on: [push]

jobs:
  try-checkout:
    runs-on: ubuntu-latest
    steps:
      - name: List files
        run: |
          pwd
          ls -a
          hostnamectl
          echo $GITHUB_SHA
          echo $GITHUB_REPOSITORY
          echo $GITHUB_WORKSPACE
          echo "${{ github.token }}"
          echo "0"
          git init
          echo "1"
          git config user.name "Munish Mehta"
          echo "2"
          git config credential.username mumehta
          echo "3"
          git config user.email munish.mehta.au@gmail.com
          echo "4"
          git config core.sshCommand "ssh -i ~/.ssh/id_ed25519"
          echo "5"
          # git clone git@github.com:$GITHUB_REPOSITORY
          git config --list
          echo "6"
